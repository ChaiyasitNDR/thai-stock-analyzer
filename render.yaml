services:
  - type: web
    name: thai-stock-analyzer
    runtime: python
    pythonVersion: "3.11.9"
    region: singapore
    buildCommand: pip install -r requirements.txt
    startCommand: sh setup.sh && streamlit run app.py
    plan: free